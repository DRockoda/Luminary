services:
  - type: web
    name: luminary-api
    runtime: node
    rootDir: apps/api
    buildCommand: npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
