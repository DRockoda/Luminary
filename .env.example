# Luminary reads env from each workspace. Copy the templates:
#
#   cp apps/api/.env.example apps/api/.env
#   cp apps/web/.env.example apps/web/.env
#
# Then edit those files — do not commit real `.env` files.
