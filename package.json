{
  "name": "luminary",
  "private": true,
  "version": "0.1.0",
  "description": "Luminary — a private space to reflect. Full-stack personal journaling app.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm-run-all -p dev:api dev:web",
    "dev:api": "npm --workspace apps/api run dev",
    "dev:web": "npm --workspace apps/web run dev",
    "build": "npm --workspace packages/shared run build && npm --workspace apps/api run build && npm --workspace apps/web run build",
    "db:push": "npm --workspace apps/api run db:push",
    "db:migrate": "npm --workspace apps/api run db:migrate",
    "db:generate": "npm --workspace apps/api run db:generate",
    "db:seed": "npm --workspace apps/api run db:seed"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
